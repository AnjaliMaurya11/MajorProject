<nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top">
  <div class="container-fluid">

    <!-- Left -->
    <div class="d-flex flex-grow-1 align-items-center">
      <a class="navbar-brand" href="/listings">
        <i class="fa-solid fa-mountain-city"></i>
      </a>

      <a class="nav-link d-none d-md-block" href="/listings">
        Explore
      </a>
    </div>

    <!-- Center Search -->
    <form class="d-flex mx-auto search-nav" method="GET" action="/listings">
      <input
        class="form-control"
        type="search"
        placeholder="Search"
        name="search"
        aria-label="Search"
        value="<%= typeof search !== 'undefined' ? search : '' %>"
      />
      <button class="search-btn" type="submit">
        <i class="fa-brands fa-sistrix fa-lg"></i>
      </button>
    </form>

    <!-- Hamburger -->
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Right Menu -->
    <div class="collapse navbar-collapse justify-content-end" id="navbarNavAltMarkup">
      <div class="navbar-nav px-2 align-items-end align-items-md-center">

        <a class="nav-link" href="/listings/new">Become a host</a>

        <% if (!currentUser) { %>
          <a href="#" data-bs-toggle="modal" data-bs-target="#signupModal" class="nav-link">
            Sign Up
          </a>

          <a href="#" data-bs-toggle="modal" data-bs-target="#loginModal" class="nav-link">
            Login
          </a>
        <% } else { %>

          <a class="nav-link" href="/bookings/mybookings">
            My Bookings
          </a>

          <a class="nav-link" href="/users/logout">
            <b>Log out</b>
          </a>

        <% } %>

      </div>
    </div>

  </div>
</nav>